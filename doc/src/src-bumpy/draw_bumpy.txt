use x, h data in draw_bumpy.dat (np.loadtxt),
-0.2 to 0.5 is fine on the y axis.
x and h are the first road profile, 0:400, from  the
three generated by generate_bumpy_road.py.

1. load into pysketcher script
2. make drawing of road and vehicle
3. add vectors for deriving the model
4. place these in fig-bumpy or in deqbook
